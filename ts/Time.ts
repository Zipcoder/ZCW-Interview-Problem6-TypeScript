class Time {

  hours: number;
  minutes: number;
  isAM: boolean;

  constructor(hours: number, minutes: number, isAM: boolean) {
    this.hours = hours;
    this.minutes = minutes;
    this.isAM = isAM;
  }
}
